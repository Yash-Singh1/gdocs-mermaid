import { library } from '@fortawesome/fontawesome-svg-core';
import {
  faCircle,
  faCircleNotch,
  faMaximize,
  faMicrophone,
  faMinimize,
  faXmark,
} from '@fortawesome/free-solid-svg-icons';

library.add(
  faCircleNotch,
  faMaximize,
  faMinimize,
  faXmark,
  faMicrophone,
  faCircle
);
